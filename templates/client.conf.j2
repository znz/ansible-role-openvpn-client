client
dev tun
proto {{ item.proto }}
remote {{ item.host }} {{ item.port }}
resolv-retry infinite
nobind
;user nobody
;group nogroup
persist-key
persist-tun
;mute-replay-warnings
ca {{ item.name }}-ca.crt
cert {{ item.name }}.crt
key {{ item.name }}.key
;ns-cert-type server
remote-cert-tls server
{% if item.has_key('ta_key') and item.ta_key != "" %}
tls-auth {{ item.name }}-ta.key 1
{% endif %}
cipher {{ item.cipher }}
auth {{ item.auth }}
{% if item.has_key('tls_cipher') and item.tls_cipher != "" %}
tls-cipher {{ item.tls_cipher }}
{% endif %}
comp-lzo
verb 3
;mute 20
